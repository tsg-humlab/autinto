{
  "name": "exercise-widget",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^5.2.3",
    "ramda": "^0.28.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "shx": "^0.3.4"
  },
  "devDependencies": {
    "prettier": "2.8.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build-widget": "react-scripts build",
    "copy-dependencies": "shx cp ./build/static/js/main.*.js ../uploads/additional-files/main.js && shx cp ./build/static/css/main.*.css ../uploads/additional-files/main.css",
    "build-book": "cd ../uploads && mdbook build --dest-dir ../static",
    "build": "npm run build-widget && npm run copy-dependencies && npm run build-book"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 firefox version",
      "last 1 chrome version",
      "last 1 safari version"
    ]
  }
}
